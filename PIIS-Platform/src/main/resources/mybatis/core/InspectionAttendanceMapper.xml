<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tech.piis.modules.core.mapper.InspectionAttendanceMapper">
    <resultMap id="unitsBizCountResultMap" type="UnitsBizCountVO">
        <result property="unitsId" column="units_id"/>
        <result property="orgId" column="org_id"/>
        <result property="orgName" column="org_name"/>
        <result property="count" column="count"/>
    </resultMap>

    <select id="selectInspectionAttendanceCount" resultMap="unitsBizCountResultMap" parameterType="java.lang.String">
        select u.UNITS_ID,u.ORG_ID,u.ORG_NAME,count(a.UNITS_ID) as count
        from inspection_units u,inspection_attendance a
        where u.UNITS_ID = a.UNITS_ID
        and u.plan_id = #{planId}
        group by a.UNITS_ID
    </select>

</mapper>