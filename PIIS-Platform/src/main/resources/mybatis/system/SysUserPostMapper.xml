<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tech.piis.modules.system.mapper.SysUserPostMapper">

    <resultMap type="tech.piis.modules.system.domain.SysUserPost" id="SysUserPostResult">
        <result property="userId" column="user_id"/>
        <result property="postId" column="post_id"/>
    </resultMap>

    <resultMap id="SysUserPostVOResult" type="tech.piis.modules.system.domain.SysUserPostVO">
        <result property="userId" column="user_id"/>
        <result property="postId" column="post_id"/>
        <result property="postName" column="post_name"/>
    </resultMap>

    <delete id="deleteUserPostByUserId" parameterType="String">
		delete from sys_user_post where user_id=#{userId}
	</delete>

    <select id="countUserPostById" resultType="Integer">
	    select count(1) from sys_user_post where post_id=#{postId}  
	</select>

    <delete id="deleteUserPost" parameterType="Long">
        delete from sys_user_post where user_id in
        <foreach collection="array" item="userId" open="(" separator="," close=")">
            #{userId}
        </foreach>
    </delete>

    <insert id="batchUserPost" parameterType="tech.piis.modules.system.domain.SysUserPost">
        insert into sys_user_post(user_id, post_id) values
        <foreach item="item" index="index" collection="list" separator=",">
            (#{item.userId},#{item.postId})
        </foreach>
        ON DUPLICATE KEY UPDATE
        user_id = values(user_id)
    </insert>

    <insert id="saveUserPost" parameterType="tech.piis.modules.system.domain.SysUserPost">
		insert into sys_user_post(user_id, post_id) values(#{userId},#{postId})
	</insert>

    <select id="selectUserPost" parameterType="tech.piis.modules.system.domain.SysUserPostVO"
            resultMap="SysUserPostVOResult">
        select sup.user_id,sup.post_id,sp.post_name
        from sys_user_post sup,sys_post sp
        <where>
            sup.post_id = sp.post_id
            <if test="userId != null and userId != ''">
                and user_id = #{userId}
            </if>
            <if test="postName != null and postName != ''">
                and post_name = #{postName}
            </if>
        </where>
    </select>

    <delete id="delUserPostBatch" parameterType="tech.piis.modules.system.domain.SysUserPost">
        delete from sys_user_post
        where user_id in (
        <foreach collection="list" item="item" separator=",">
            #{item.userId}
        </foreach>
        )
    </delete>
</mapper> 